{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | { read -r f; if [[ \"$f\" == *.ipynb ]]; then echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"You just read a Jupyter notebook. Invoke the cell-docs skill now to check for undocumented code cells and add spec cells where missing.\"}}'; fi; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
